{
  "name": "bun-template-repo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "python3 ./node_modules/deploy/main.py --configdeploy.yaml",
    "type-check": "bun --bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "deploy": "https://github.com/fan-Pier-Labs/deploy"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
